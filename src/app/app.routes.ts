import { Routes } from '@angular/router';
import { AddProductReactive } from './pages/add-product-reactive/add-product-reactive';

export const routes: Routes = [
    {
        path: 'add-product',
        component: AddProductReactive
    }
];
